<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taoxier.taoxiblog.mapper.TagMapper">

    <!--按博客id查询List-->
    <select id="getTagListByBlogId" resultType="com.taoxier.taoxiblog.model.entity.Tag">
        select t.tag_name, t.color
        from blog_tag as bt left join tag as t on bt.tag_id=t.id
        where bt.blog_id=#{blogId}
    </select>

    <!--查询每个标签的博客数量-->
    <select id="getTagBlogCount" resultType="com.taoxier.taoxiblog.model.vo.TagBlogCountVO">
        select tag_id, count(tag_id) as blog_count from blog_tag group by tag_id
    </select>


</mapper>